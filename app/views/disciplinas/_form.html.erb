<%= form_with(model: disciplina) do |form| %>
  <% if disciplina.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(disciplina.errors.count, "error") %> prohibited this disciplina from being saved:</h2>

      <ul>
        <% disciplina.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_area :descricao %>
  </div>

  <div>
    <%= form.label :local, style: "display: block" %>
    <%= form.text_field :local %>
  </div>

  <div class="field">
    <%= form.label :dias_semana, "Dias da Semana" %>
    <% ["Segunda", "Terça", "Quarta", "Quinta", "Sexta"].each do |dia| %>
      <div class="form-check">
        <%= form.check_box :dias_semana, { multiple: true }, dia, nil %>
        <%= form.label "disciplina_dias_semana_#{dia.downcase}", dia %>
      </div>
    <% end %>
  </div>

  <div class="actions">
    <%= form.submit "Salvar" %>
  </div>
<% end %>